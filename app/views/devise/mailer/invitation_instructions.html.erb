<div id="email-wrapper">
  <div id="header">
    <!-- <td width="40px"><%#= email_image_tag "img/logo.png", :class => "logo" %></td> -->
      <h3>Hello <%= @resource.first_name %> <%= @resource.last_name %>!</h3>
  </div>
  <div id="body">
    <p>W're happy to have you on board in the Stacey community.</p>
    <p>The following link allows you to create an Account on our community page.</p>
    <p>The benefits of our page is that you'll be able to get in contact with us and your co-living partners as easy and fast as possible.</p>
    <p>Click on the link to create your Account.</p>
    <%= link_to 'Join STACEY Community', accept_invitation_url(@resource, :invitation_token => @token) %>
  </div>
</div>

<!-- <p class="invitation-email-header">Hello <%= @resource.first_name %> <%= @resource.last_name %>,</p>

<p>Matteo invited you to join the Stacey Community.</p>

<p><%#= link_to t("devise.mailer.invitation_instructions.accept"), accept_invitation_url(@resource, :invitation_token => @token) %></p>

<#% if @resource.invitation_due_at %>
  <p><%#= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %></p>
<#% end %>

<p><%#= t("devise.mailer.invitation_instructions.ignore") %></p>
