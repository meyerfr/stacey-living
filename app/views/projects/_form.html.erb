<%= simple_form_for project, html: { class: 'new-form' } do |builder| %>
  <%= render 'project_form_fields', f: builder, amenities: amenities, project: project, last_project: last_project %>
<% end %>
